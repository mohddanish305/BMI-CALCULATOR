import 'package:flutter/material.dart';

class AppColors {
  // Light Mode
  static const lightBg = Color(0xFFF5F6FF);
  static const lightCard = Colors.white;
  static const lightText = Color(0xFF111827);

  // Dark Mode
  static const darkBg = Color(0xFF0B1220);
  static const darkCard = Color(0xFF121B2E);
  static const darkText = Colors.white;

  // Common
  static const primary = Color(0xFF6C63FF);
  static const slider = primary;

  // BMI colors (optional)
  static const underweight = Colors.blue;
  static const normal = Colors.green;
  static const overweight = Colors.orange;
  static const obese = Colors.red;
}
